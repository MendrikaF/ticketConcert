<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/styles.css">

    <title>Réservation - Page 5</title>
</head>
<body>
    <h2>Merci pour votre réservation !</h2>
    <p>Numéro de ticket : <span id="ticketNumber">#CON001234</span></p>
    <p>Nombre de places réservées : <span id="seatsNumber"><%= seats %></span></p>

    <h3>Montant à payer : <span id="amount"><%= seats * 15 %> €</span></h3>

    <form action="/payment" method="POST">
        <label for="paymentMethod">Méthode de paiement :</label>
        <select id="paymentMethod" name="paymentMethod">
            <option value="creditCard">Carte de crédit</option>
            <option value="paypal">PayPal</option>
            <option value="paypal">Mvola</option>
            <option value="paypal">Visa</option>
        </select>
        <br><br>
        <button type="submit">Payer</button>
    </form>

    <script>
        // Simuler un paiement
        document.querySelector('form').onsubmit = function(event) {
            event.preventDefault(); // Empêche l'envoi du formulaire pour simuler un paiement
            setTimeout(function() {
                alert("Merci pour votre paiement !");
                window.location.href = '/thank-you'; // Page de remerciement ou confirmation
            }, 2000);
        };
    </script>
</body>
</html>
