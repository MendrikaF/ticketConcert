<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/styles.css">

    <title>Réservation - Page 3</title>
</head>
<body>
    <h2>Votre réservation est en cours...</h2>
    <p>Temps d'attente : <span id="countdown">15</span> secondes</p>
    <p>Numéro de votre ticket : <span id="ticketNumber">#CON001234</span></p>

    <button id="yourTurnButton" style="display: none;" onclick="window.location.href='/page4'">Votre tour</button>

    <script>
        let countdown = 15;
        const countdownElement = document.getElementById("countdown");
        const yourTurnButton = document.getElementById("yourTurnButton");

        const interval = setInterval(() => {
            countdown--;
            countdownElement.textContent = countdown;

            if (countdown === 0) {
                clearInterval(interval);
                yourTurnButton.style.display = "block"; // Afficher le bouton "Your Turn"
            }
        }, 1000);
    </script>
</body>
</html>
